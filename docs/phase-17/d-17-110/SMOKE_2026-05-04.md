# D-17-110 Smoke Tests — 2026-05-04

**Deliverable:** D-17-110 — DeepSeek dual-loop verifier integration (Layer 1.5)
**Status at test time:** WP-04 complete (verifier authored), WP-05 complete (integrated into aider-task.sh)
**Verifier model:** `qwen2.5-coder:14b` on Mac Mini local Ollama (127.0.0.1:11434)
**Note:** `deepseek-coder-v2:16b-lite-instruct-q4_K_M` was pulled to Mac Studio but is not externally reachable (D-17-109 WP-03 LaunchDaemon deferred). `qwen2.5-coder:14b` validated as instruct-following replacement for Mac Mini execution.

---

## Test 1 — AGREE expected (docstring correctly added)

**Description:** `Add a docstring to the parse_config function`
**Diff:** Single `+` line adding `"""Parse YAML configuration file and return a dict."""` inside `parse_config`
**Function count:** unchanged (synthetic — no real file)

**Result:**
```
[aider-verifier] dual-loop=AGREE
[aider-verifier] REASON: The diff adds a docstring to the `parse_config` function as requested by the task description. No other changes were made.
```
**Exit:** 0
**Status:** PASS

---

## Test 2 — DISAGREE expected (607-line duplication pattern)

**Description:** `Add docstrings to all functions in codex51_trusted_pattern_intake.py`
**Diff:** Synthetic — all 3 function bodies duplicated, count forced 3→6
**Model sees:** `WARNING: Function/class count changed from 3 to 6.` in prompt

**Result (qwen2.5-coder:14b):**
```
VERDICT: DISAGREE
REASON: The function count changed unexpectedly from 3 to 6, and the function bodies are duplicated. This indicates that more changes were made than just adding docstrings.
```
**Exit:** 1
**Status:** PASS

**Note:** `deepseek-coder-v2:latest` (base chat, not instruct) failed this test — AGREEd despite warning. Confirmed `qwen2.5-coder:14b` (instruct) is required for reliable DISAGREE classification.

---

## Test 3 — DISAGREE expected (logic rewritten beyond scope)

**Description:** `Fix the indentation of the error message on line 47`
**Diff:** `requests.get(url, timeout=5)` → timeout changed to 30 AND entire exception block rewritten with multi-branch logic

**Result:**
```
[aider-verifier] dual-loop=DISAGREE
[aider-verifier] REASON: The diff modifies the logic of the function beyond just fixing the indentation of the error message on line 47. It changes the timeout value and adds additional error handling logic.
```
**Exit:** 1
**Status:** PASS

---

## Test 4 — Graceful failure (model unreachable)

**Description:** Pointed to non-existent endpoint `http://127.0.0.1:19999`

**Result:**
```
[aider-verifier] ERROR: Ollama unreachable at http://127.0.0.1:19999 (attempt 2): <urlopen error [Errno 61] Connection refused>
```
**Exit:** 2
**Status:** PASS — verifier exits 2 (error), aider-task.sh continues (exit 2/3 are non-blocking)

---

## Model Evaluation Summary

| Model | AGREE | DISAGREE (dup) | DISAGREE (logic) | Verdict |
|---|---|---|---|---|
| `deepseek-coder-v2:latest` (Mac Mini base) | PASS | FAIL (AGREEd) | FAIL (AGREEd) | **Rejected** |
| `qwen2.5-coder:14b` (Mac Mini instruct) | PASS | PASS | PASS | **Selected** |
| `deepseek-coder-v2:16b-lite-instruct-q4_K_M` (Mac Studio) | untested (not externally reachable) | — | — | Candidate when WP-03 lands |

---

## WP-02 Pull Verification

```
deepseek-coder-v2:16b-lite-instruct-q4_K_M    dac6ff6589c9    10 GB    (Mac Studio)
```
**Digest (sha256[:12]):** `dac6ff6589c9`
**Architecture:** `deepseek2`
**Stop tokens:** `User:` / `Assistant:`
**Provenance:** `scan-failed` (scan_rc=137, OOM kill during fingerprint scan — same pattern as Qwen3-Coder-30B-A3B-Instruct). Accepted under operator risk acceptance per model-provenance-doctrine.md precedent.

---

## Integration Test (aider-task.sh --dry-run)

```
scripts/aider-task.sh --quiet --class C0 --dry-run "Add docstring to function X" bin/level10_qualify.py
[aider-task] route=local_aider model=qwen2.5-coder:14b conf=0.78 class=C0
[aider-task] system-context injected
[aider-task] dry-run: would invoke bin/aider_local.sh with: ...
```
**Status:** PASS — dry-run works, Layer 1.5 block only triggers post-actual-run (not in dry-run path)
