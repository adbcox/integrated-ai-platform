# D-17-34 — Home Assistant architecture reconciliation — CLOSEOUT

**Date closed:** 2026-05-03
**Phase:** 17
**Trigger:** Operator-flagged regression — Phase-15 audit (`docs/phase-15/COMPREHENSIVE_AUDIT_VALIDATION_2026-05-01.md` §4.7) recommended Mac Mini HA container retirement on 2026-05-01; operator confirmed in conversation; recommendation was never executed. Container kept running for ~48h with health checks green and CMDB still listing it. D-17-34 closes the gap.

---

## What was true at the start

- Two Home Assistant footprints existed: a stray container `homeassistant` on Mac Mini (172.23.0.3:8123, healthy, REST sensors broken since mediahog port reorg) and the canonical hub at 192.168.10.141 (Caddy `home.internal` proxies here).
- Mac Mini container had zero live consumers (no `depends_on`, no `depended_on_by`, no inbound Caddy route).
- Vault AppRole `homeassistant` + policy `homeassistant-policy` + on-disk role-id/secret-id + Vault Agent secret directory all existed (provisioned 2026-04-29 → 2026-04-30); credentials were live but never delivered to a running consumer (Vault Agent sidecar absent).
- `dependency-graph.md` showed `homeassistant-container` + 2 dead Obot shim refs (`obot-shim-homeassistant`, `obot-shim-homeassistant-nanobot` — not running as containers).
- `ARCHITECTURE.md` carried the integration-container row.
- CLAUDE.md hardware block did not mention .141 (only Mac Mini + Studio + MacBook + QNAP).

## What was done

### WP-04 (execution, in plan order)

1. **Pre-flight log verification** (already complete during WP-02): confirmed REST sensor errors only — no surprise activity, no human interaction, recorder DB growing only from Phase-7 sensor poll attempts.

2. **QNAP archive** of `dashboards_homeassistant-config` volume (9.8 MB on disk → 3.9 MB tar.gz):
   - Path: `/share/CACHEDEV2_DATA/downloads/manual/D-17-34-ha-container-retirement/` (reachable via SMB at `/Users/admin/mnt/qnap-downloads/manual/D-17-34-ha-container-retirement/`).
   - Files: `dashboards_homeassistant-config-pre-D-17-34-retirement-2026-05-03T13-21-49Z.tar.gz` + `.meta.json` sidecar.
   - SHA256: `4fe65026aa6d0c81831f229dec4625426e46b43d006580016c46e1d2b9b5f173` (verified post-transfer via SMB).
   - Retention: 90 days, expires 2026-08-01.
   - **Plan deviation noted:** operator's planned path `/share/restic-backups/manual/` does not exist on the QNAP (restic uses MinIO/S3 endpoint, not a filesystem path). The closest match `/share/CACHEDEV2_DATA/downloads/manual/` was used. See Surface-Back log.

3. **Container stop + remove** (`docker stop homeassistant && docker rm homeassistant`); compose `homeassistant:` block + `volumes:` declaration commented out in `~/control-center-stack/stacks/dashboards/docker-compose.yml` with retirement marker referencing D-17-34 (original block preserved in comment form for forensics). Local volume `dashboards_homeassistant-config` retained as defense-in-depth alongside QNAP archive.

4. **Registry refresh** (`scripts/platform-registry/refresh.sh`): `~/.platform-registry/by-service/homeassistant.json` removed; `inventory.json` no longer carries the container record (Caddy route entry for `homeassistant.internal` → 192.168.10.141:8123 retained — that's the canonical .141 hub, unrelated to the retired container).

5. **Vault cleanup**:
   - AppRole `homeassistant` deleted via `vault delete auth/approle/role/homeassistant`.
   - Policy `homeassistant-policy` deleted via `vault policy delete homeassistant-policy`.
   - On-disk dirs removed: `~/.vault-approle/homeassistant/`, `~/.vault-agent-secrets/homeassistant/`.
   - Repo policy file removed: `config/vault-policies/homeassistant-policy.hcl` (`git rm`).
   - `secret/homeassistant/api` retained (canonical .141 hub may consume it; out-of-scope per operator instruction).

6. **CMDB updates**:
   - `docs/architecture/dependency-graph.md`: dropped `homeassistant-container` node + 2 dead Obot shim nodes + 3 dead edges; updated `homeassistant-physical` label to "192.168.10.141 (canonical)".
   - `docs/ARCHITECTURE.md`: dropped "Home Assistant (container) | 8123 | HA integration container" row from §Platform Services; updated Dashboards stack row to "(empty — Home Assistant retired 2026-05-03 per D-17-34; canonical HA at 192.168.10.141)".
   - `CLAUDE.md` Hardware block: added explicit `.141` HA hub entry with note that Mac Mini did not and does not host an HA container.

### WP-05 (doctrine)

Three findings authored into `docs/architecture-facts/integration-audit-doctrine.md`:

- **Finding 2 — F7 false-positive completion regression.** Audit recommendations require executed validation, not conversation agreement. Empirical examples: Sonarr/QNAP SMB mount break (D-17-32 Gap F5) + this HA container case. Sub-doctrine: health-check signal hides un-executed retirements ("container `(healthy)` is not `(supposed to exist)`"), compounding the D-17-32 sub-doctrine.
- **Finding 3 — Stranded Vault AppRoles outlive their consumers.** Five-artifact-class cleanup checklist documented; mirror failure mode to D#25 plane-web example (orphan-after-removal vs orphan-from-provisioning). Follow-through complete: canonical runbook now exists at `docs/runbooks/retire-service.md` (D-17-57).
- **Finding 4 — CMDB authority is unenforced.** Three substrates can disagree silently (NetBox / `service-registry.yaml` / `~/.platform-registry/inventory.json`). Reconciliation deliverable proposed but NOT auto-created per operator instruction; cross-references D-17-32 Gap X1 (registry agent surface).

## What was NOT done (per operator constraint)

- No touches to .141 (canonical HA hub).
- No deletion of the QNAP archive (90-day retention stands).
- No deletion of local `dashboards_homeassistant-config` volume (defense-in-depth retention).
- No deletion of `secret/homeassistant/api` Vault data (potential .141 consumer).
- No auto-creation of CMDB reconciliation deliverable (Finding 4 is proposed only — operator decides scope in next planning pass).
- No auto-creation of `retire-service.md` runbook (Finding 3 follow-up — proposed only).

## Surface-Back log

Per operator instruction, surfaced back at WP-04 step 3 (pre-container-stop checkpoint) before destructive actions. Reported:

- Archive integrity verified (SHA256 match post-transfer).
- Path deviation from operator plan (`/share/restic-backups/manual/` does not exist on this QNAP; used `/share/CACHEDEV2_DATA/downloads/manual/D-17-34-ha-container-retirement/` — only manual-archive convention present).
- Nothing unexpected in pre-flight (no live consumers, no surprise activity).
- No further surprises during the remaining WP-04 steps.

## Cross-references

- Originating audit: `docs/phase-15/COMPREHENSIVE_AUDIT_VALIDATION_2026-05-01.md` §4.7 (un-executed recommendation).
- Companion audit: `docs/_audit/integrated-stack-gaps-2026-05-03.md` (D-17-32 Gap F5 — empirical example #1 for F7 pattern).
- Doctrine chronicle: `docs/architecture-facts/integration-audit-doctrine.md` Findings 2-4.
- Pre-retirement archive: QNAP `/share/CACHEDEV2_DATA/downloads/manual/D-17-34-ha-container-retirement/` (90-day retention, expires 2026-08-01).

## Phase-17 progression

26/28 deliverables DONE after this close (was 25/28 after D-17-32).
