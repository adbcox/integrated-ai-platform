# Local AI Stack Role Matrix

| System | Role in this wave | Operational mode | Authority boundary | Status |
|---|---|---|---|---|
| OpenHands | Local coding session surface | Docker launch wrapper | Cannot override roadmap/gates | Implemented wrapper |
| MarkItDown | Local ingestion converter | Local venv + thin wrapper | Ingestion utility only | Implemented wrapper |
| MCP reference servers | MCP capability probes | Allowlist launch wrapper | Reference/test only, no hidden backbone | Implemented wrapper |
| PR-Agent | PR review automation helper | Local venv CLI + disabled workflow template | PR helper only, no planner authority | Implemented wrapper |
| n8n | Workflow orchestration candidate | Evaluation-only note | Deferred pending fit criteria | Evaluated/deferred |
