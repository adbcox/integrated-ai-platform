# Autonomous Ready Queue

This document mirrors the latest machine-readable governed queue:

- `artifacts/planning/next_pull.json`

## Current queue snapshot

- Selector status: `no_eligible_targets`
- Chosen next item: `none`
- Eligible candidate count: `0`
- Blocked by dependency: `none`
- Blocked placeholder state: `none`

For execution, always consume the machine-readable queue artifact directly.
