# CMDB Linkage

CMDB-lite remains the architecture/runtime authority layer.

Roadmap items may reference CMDB-lite through canonical refs such as:

- `subsystem:roadmap_ops`
- `subsystem:control_plane`
- `subsystem:agent_runtime`

Roadmap records may link to CMDB authority, but roadmap execution must not require runtime CMDB writeback.
